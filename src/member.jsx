import React from 'react';
import ReactDOM from 'react-dom/client';
import MemberCenterApp from './MemberCenterApp.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <MemberCenterApp />
  </React.StrictMode>
);
